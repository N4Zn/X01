<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools" package="com.unity3d.player" android:versionCode="1" android:versionName="1.0">
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.VIBRATE" />
  <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
  <!-- FRTest: USB UVC camera + face recognition DB -->
  <uses-permission android:name="android.permission.CAMERA" />
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
  <uses-feature android:name="android.hardware.usb.host" android:required="false" />
  <application android:label="@string/app_name" android:icon="@mipmap/app_icon" android:theme="@android:style/Theme.NoTitleBar.Fullscreen" android:hardwareAccelerated="true">
    <!--
      EduXplore 2.0 — Floor Projection App
      Landscape locked, multi-touch, fullscreen.
      App thường (không phải launcher).
      Dùng UnityPlayerActivity (class thực tế trong build Unity 2022.3 IL2CPP).
    -->
    <!-- Track A (EduXplore 2.0): KHÔNG còn là entry point/HOME — ControlActivity
         (com.eduxplore.control, từ controlui-release.aar) giờ là launcher. UnityPlayerActivity
         chỉ được khởi chạy tường minh (ControlActivity.startGame(), Intent.setClassName),
         đẩy sang display phụ qua ActivityOptions.setLaunchDisplayId() khi có máy chiếu, hoặc
         display 0 nếu không (xem ControlActivity.java). exported=true để việc đó gọi được. -->
    <activity android:name="com.unity3d.player.UnityPlayerActivity" android:screenOrientation="landscape" android:launchMode="singleTask" android:configChanges="mcc|mnc|locale|touchscreen|keyboard|keyboardHidden|navigation|orientation|screenLayout|uiMode|screenSize|smallestScreenSize|fontScale|layoutDirection|density" android:exported="true">
      <!-- USB_DEVICE_ATTACHED intent-filter and usb_device_filter.xml are declared
           inside unityplugin.aar (manifest merge) to avoid the obsolete res/ folder error. -->
    </activity>
    <!-- FileProvider cho AppUpdater: chia sẻ APK với Android installer -->
    <provider android:name="androidx.core.content.FileProvider" android:authorities="com.EduXplore.X01a.fileprovider" android:exported="false" android:grantUriPermissions="true">
      <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_provider_paths" />
    </provider>
  </application>
</manifest>