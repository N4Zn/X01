<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!-- Auto-grant USB permission khi Lidar (CP2102) được cắm — không hiện dialog,
             khớp pattern usb_device_filter đã kiểm chứng trong MyNativeApp_v4. -->
        <activity
            android:name=".LidarUsbAttachActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/lidar_usb_device_filter" />
        </activity>
    </application>
</manifest>
